{% extends 'adminbase.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('user_index') }}">Geri Dön</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    <div class="float-right"> {{ include('admin/user/_delete_form.html.twig') }}</div>

    {{ include('admin/url/urls.html.twig') }}
{% endblock %}
